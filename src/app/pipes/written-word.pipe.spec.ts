import { WrittenWordPipe } from './written-word.pipe';

describe('WrittenWordPipe', () => {
  it('create an instance', () => {
    const pipe = new WrittenWordPipe();
    expect(pipe).toBeTruthy();
  });
});
