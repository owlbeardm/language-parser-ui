import { KeySymbolsPipe } from './key-symbols.pipe';

describe('KeySymbolsPipe', () => {
  it('create an instance', () => {
    const pipe = new KeySymbolsPipe();
    expect(pipe).toBeTruthy();
  });
});
