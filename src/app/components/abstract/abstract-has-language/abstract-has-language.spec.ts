import { AbstractHasLanguage } from './abstract-has-language';

describe('HasLanguage', () => {
  it('should create an instance', () => {
    expect(new AbstractHasLanguage()).toBeTruthy();
  });
});
