import { AbstractHasLanguageComponent } from './abstract-has-language.component';

describe('HasLanguage', () => {
  it('should create an instance', () => {
    expect(new AbstractHasLanguageComponent()).toBeTruthy();
  });
});
